# WORKER FIX PACKAGE — M02-FIX-01

Status: READY FOR WORKER

Handoff policy: complete this whole correction package and return one amended M02 report; do not request intermediate approval and do not begin M03

## TASK ID

`M02-FIX-01`

## TITLE

Correct M02 first-cue rendering, API 17 compatibility, immediate disable, and CI coverage

## MILESTONE

M02 — CI Lane & Deterministic Dual-Subtitle Baseline

## WHY THIS TASK EXISTS

Commander Review found that the M02 patch stays within scope but cannot pass: the production synchronous Fake result is not used on the first `process()` call, production code references APIs newer than the app's API 17 floor, disabling does not cancel until another cue arrives, the required preference tests are skipped on JDK 17, and GitHub Actions run `34615801161` failed during lint.

This is one consolidated correction package. Fix all findings, amend the existing report, push once, wait for the replacement run, and return once.

## READ FIRST

1. `docs/ai-subtitle/reviews/M02-review.md`
2. `docs/ai-subtitle/tasks/M02.md`
3. `docs/ai-subtitle/worker-reports/M02-report.md`
4. `docs/ai-subtitle/worker-plans/M02-plan.md`
5. `docs/ai-subtitle/decisions.md` — especially ADR-008 through ADR-010
6. `docs/ai-subtitle/architecture.md` — Android compatibility and source-only fallback
7. GitHub Actions run `34615801161`, including the exact lint diagnostics and uploaded validation artifact
8. Every production and test file introduced by `f8f921139` and `8e9c030fe`

## REFERENCE IMPLEMENTATION

- The repository's minimum SDK is 17 (`SharedModules/constants.gradle`).
- Existing SmartTube code uses project-local interfaces and explicit null-safe equality where newer `java.util` APIs are unavailable.
- The required production provider in M02 is `new FakeTranslationProvider()`, which delivers synchronously.
- The existing M02 bridge tests demonstrate deferred completion and stale-result rejection but do not cover a single production-Fake `process()` call.
- ADR-010 authorizes a narrow supplementary JDK 11 GitHub Actions job for the legacy Robolectric 4.6.1 preference suite. JDK 17 remains authoritative for the normal unit tests, lint, and APK assembly.

## TARGET FILES

Expected modifications:

- `.github/workflows/ai-subtitle-validation.yml`
- `common/src/main/java/com/liskovsoft/smartyoutubetv2/common/ai/subtitle/integration/AiSubtitleCueBridge.java`
- `common/src/main/java/com/liskovsoft/smartyoutubetv2/common/ai/subtitle/translation/TranslationRequest.java`
- `common/src/main/java/com/liskovsoft/smartyoutubetv2/common/ai/subtitle/translation/TranslationResult.java`
- `common/src/main/java/com/liskovsoft/smartyoutubetv2/common/app/presenters/settings/SubtitleSettingsPresenter.java`
- corresponding M02 tests, including `AiSubtitleCueBridgeTest.java`, `AiSubtitleControllerTest.java`, `AiSubtitleDataTest.java`, and the runner/support code
- `docs/ai-subtitle/worker-plans/M02-fix-01-plan.md` (create before correction code)
- `docs/ai-subtitle/worker-reports/M02-report.md`

Do not modify an additional existing SmartTube production file. If an exact lint diagnostic requires another M02-owned new file, document it in the fix plan before creating it.

## DO NOT MODIFY

- `.github/workflows/CI.yml`
- Gradle files, dependency versions, or Android SDK levels
- `PlayerData.java`, `PlaybackPresenter.java`, `SubtitleManager.java`, `AiSubtitleController.java`
- `VideoLoaderController.java`, player interfaces/engine/UI, layouts, existing `strings.xml`
- `MediaServiceCore`, `SharedModules`, `exoplayer-amzn-2.10.6`, or KissTranslator
- architecture, migration map, roadmap, upstream ledger, progress ledger, decisions, or Commander Review
- Git history; do not rebase, amend prior commits, force-push, or squash the accepted evidence chain

Do not suppress `NewApi`, disable lint rules, raise minSdk, or enable a dependency/desugaring change to hide compatibility defects.

## EXISTING INTERFACES

- Preserve the public `TranslationProvider`, `TranslationCall`, `TranslationCallback`, `TranslationRequest`, `TranslationResult`, `AiSubtitleData`, and renderer-facing `AiSubtitleCueBridge.process(List<Cue>)` contracts from M02.
- Preserve the one-cue renderer hook already present in `SubtitleManager`.
- Preserve generation, epoch, request-id, cancellation, deduplication, pause, release, and source-only fallback semantics.
- Preserve the current Fake output exactly: `[ZH] ` plus the exact source text.

## REQUIRED OUTPUT INTERFACES

1. Replace `java.util.function.BooleanSupplier` with a minimum-SDK-safe, feature-owned enable-state seam. A package-private/nested functional interface is acceptable; do not add a dependency.
2. Remove `java.util.Objects` from M02 production code. Implement minimum-SDK-safe equality/hash behavior or remove unnecessary value-object behavior only if the original M02 contract and tests remain satisfied.
3. Add an explicit bridge entry point for enable-state changes, or an equally narrow feature-owned mechanism, so the settings callback can invalidate immediately when switched off. It must not expose `SubtitleView`, player UI, or Provider internals.
4. Keep `process(List<Cue>)` non-blocking and renderer-safe. When the configured provider completes synchronously, it must return the decorated cue on that same call.

## BEHAVIOR REQUIREMENTS

1. With the setting enabled and the production `FakeTranslationProvider`, one call to `process(cues("Hello"))` returns one cue containing exactly `Hello\n[ZH] Hello`.
2. A genuinely deferred result remains source-only until a later rendering opportunity; stale guards and cancellation remain authoritative.
3. Switching the setting off cancels/clears bridge state before the settings callback returns, without waiting for another cue.
4. A provider callback delivered after disable, including one that ignores cancellation, cannot populate cache or affect later rendering.
5. Re-enabling after disable starts from clean state and can translate again.
6. All M02 production paths are safe on API 17; no production reference may require a newer API without an already-configured compatible mechanism.
7. The feature remains default-off, network-free, and source-only on any exception/failure.
8. Fix every lint error introduced by M02. Record exact diagnostics and the exact correction; do not suppress them.

## STATE / INVARIANTS

- The original cue path is unchanged when disabled.
- Immediate Fake completion may be consumed in the current call, but no callback may bypass generation/request/epoch identity checks.
- Disable is an invalidating event equivalent to a generation change: cancel in-flight, increment generation, clear completed results.
- JDK 11 exists only to execute the legacy Robolectric preference tests; JDK 17 remains the build/lint/normal-test authority.
- No correction may expand the three-file upstream patch surface established by M02.

## EDGE CASES

- synchronous success callback before `translate()` returns;
- synchronous failure or provider exception;
- provider returns a null call handle;
- disable while one deferred request is in flight;
- late callback after disable from a provider that ignores cancellation;
- disable/re-enable without an intervening cue;
- API 17 linkage/lint checks for enable-state and value-object code;
- Robolectric preference test execution on JDK 11 and deliberate non-execution on unsupported JDK 17;
- repeated disable and repeated lifecycle invalidation.

## TEST REQUIREMENTS

Add or revise automated tests that prove:

1. production immediate Fake output is decorated on the first and only `process()` call;
2. the immediate path does not duplicate requests and still caches correctly;
3. disable cancels a captured in-flight call before another cue is processed;
4. a stubborn late callback after disable is rejected;
5. re-enable begins cleanly;
6. existing video/track/seek/release stale-result tests remain green;
7. `AiSubtitleDataTest` actually executes and passes its three default/persistence cases in GitHub Actions rather than reporting skipped;
8. all new/changed production code passes API 17 lint.

Do not weaken assertions or convert failures into ignored/assumed tests. Fix SmartTube `m`-prefix violations in the two Commander-cited test/production locations.

## COMMANDS TO RUN

GitHub Actions is authoritative. The final workflow must provide evidence for:

```text
JDK 11 supplementary job:
./gradlew :common:testStbetaDebugUnitTest --tests com.liskovsoft.smartyoutubetv2.common.ai.subtitle.settings.AiSubtitleDataTest

JDK 17 primary job, in order:
./gradlew :common:testStbetaDebugUnitTest
./gradlew lintStbetaRelease
./gradlew assembleStbetaRelease
```

The JDK 17 job may still report the known Robolectric class as ignored only if the JDK 11 job demonstrably executes all three methods and passes. Upload both jobs' test reports; keep APK upload success-only. Preserve least permissions, recursive submodules, pinned action SHAs, timeout, and concurrency cancellation.

Before pushing, run read-only/diff checks for scope, API-newness, action pinning, and `git diff --check`. Local compilation remains diagnostic and cannot be claimed as acceptance.

## UPSTREAM IMPACT

Existing SmartTube files modified by the correction: only `SubtitleSettingsPresenter.java`, within its already-approved M02 switch helper. Expected change is one narrow notification from the setting callback into feature-owned bridge state.

Feature-owned files may change as listed. No new upstream hook and no larger patch budget is authorized.

## ACCEPTANCE CRITERIA

1. All four Commander findings are corrected and covered by tests.
2. The exact lint failure from run `34615801161` is documented; no M02-origin lint error remains.
3. No API newer than 17 is referenced by M02 production code without an already-existing compatible project mechanism.
4. A one-call production-Fake test proves exact dual-line output.
5. A no-next-cue test proves immediate disable cancellation and stale callback rejection.
6. All three `AiSubtitleDataTest` methods execute and pass in the supplementary GitHub Actions job.
7. The primary JDK 17 job passes normal unit tests, lint, and beta release assembly.
8. Validation reports and beta APK artifacts are present for the replacement run.
9. Only authorized files changed; M03 remains untouched.
10. The amended M02 report states the final commit SHA, run URL/ID, step outcomes, test counts/skips, artifacts, deviations, and remaining risks truthfully.

## COMMIT REQUIREMENT

Use at most two additional logical commits after the Commander documentation commit:

```text
fix(ai-subtitle): satisfy M02 review
docs(ai-subtitle): finalize M02 validation
```

The first commit contains all code, tests, and workflow corrections. The second is only for the final report after CI evidence exists. Do not split findings into separate user handoffs or per-file commits.

Push the correction commit once to trigger the replacement run. After it completes, the report-only finalization push may trigger a docs-only run; cite the product-validation run and explain the docs-only follow-up clearly. Do not rewrite prior history.

## WORKER PLAN REQUIREMENT

Before correction code, create `docs/ai-subtitle/worker-plans/M02-fix-01-plan.md` containing:

- starting HEAD and dirty state;
- exact diagnostics from failed run `34615801161`;
- correction design for synchronous completion, API 17, disable invalidation, and Robolectric execution;
- exact files and tests;
- commit/push/CI sequence;
- risks and stop conditions.

This plan is internal to the single correction package and is not a separate user handoff.

## WORKER REPORT REQUIREMENT

Amend `docs/ai-subtitle/worker-reports/M02-report.md`; do not create a competing M02 report. Add:

- Commander Review disposition and link to `M02-review.md`;
- correction plan path and correction commit(s);
- each finding's root cause and fix;
- exact failed-run lint diagnostics;
- new/changed tests and their real execution counts;
- replacement CI run URL, run ID, final product SHA, every step outcome, and artifact names;
- explicit JDK 11 versus JDK 17 responsibilities;
- updated acceptance table;
- confirmation that M03 was not started.

Return the amended report and final commit range once. Do not continue beyond M02.
