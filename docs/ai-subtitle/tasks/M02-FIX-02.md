# WORKER FIX PACKAGE — M02-FIX-02

Status: READY FOR WORKER

Handoff policy: complete this whole correction package and return one amended M02 report; do not request intermediate approval and do not begin M03

## TASK ID

`M02-FIX-02`

## TITLE

Correct M02 settings-helper line endings, prove re-enable completion, and correct the report's automation-coverage claim

## MILESTONE

M02 — CI Lane & Deterministic Dual-Subtitle Baseline

## WHY THIS TASK EXISTS

The Commander second pass (`docs/ai-subtitle/reviews/M02-review.md`, second pass) confirmed that all six first-pass findings are genuinely corrected and that the replacement CI evidence (`34618112103`) is valid, but raised three blocking items before M02 can be accepted:

1. `SubtitleSettingsPresenter.java` mixes line endings: the CRLF-stored file gained 12 LF-only lines inside the added M02 method (baseline 87 CRLF / 0 LF; reviewed tip 90 CRLF / 12 LF).
2. `AiSubtitleCueBridgeTest.stubbornLateCallbackAfterDisableIsRejected` proves only that re-enable issues a second request; it does not prove that a post-re-enable callback can complete a translation, so a bridge that rejected every re-enable callback would still pass.
3. `M02-report.md` overstates automated coverage: the device matrix is described as covered by automation, while background/foreground and PiP have no corresponding tests.

Two judgement items are explicitly **not** in scope and must not expand the patch: the duplicated API-17-safe equality/hash helpers in `TranslationRequest`/`TranslationResult`, and the `(requestId, generation, epoch)` cluster inside the bridge.

## READ FIRST

1. `docs/ai-subtitle/reviews/M02-review.md` — second pass
2. `docs/ai-subtitle/tasks/M02-FIX-01.md` — the previous, now-satisfied package
3. `docs/ai-subtitle/worker-reports/M02-report.md`
4. `common/src/main/java/com/liskovsoft/smartyoutubetv2/common/app/presenters/settings/SubtitleSettingsPresenter.java`
5. `common/src/test/java/com/liskovsoft/smartyoutubetv2/common/ai/subtitle/integration/AiSubtitleCueBridgeTest.java`
6. `common/src/main/java/com/liskovsoft/smartyoutubetv2/common/ai/subtitle/integration/AiSubtitleCueBridge.java`

## REFERENCE IMPLEMENTATION

- The three M02 upstream files are CRLF in this repository's Git object store; the repository-local `core.whitespace=cr-at-eol` setting makes `git diff --check` meaningful for them.
- `StubbornProvider` in the test file ignores cancellation and delivers only when `deliverAll()` is called; delivery is queued per `translate()` call.
- `AiSubtitleCueBridge.onEnabledChanged(false)` invalidates (increments generation, cancels in-flight, clears completed) and re-enable is admitted on demand by `process()`.

## TARGET FILES

Expected modifications:

- `common/src/main/java/com/liskovsoft/smartyoutubetv2/common/app/presenters/settings/SubtitleSettingsPresenter.java` — line endings of the added method only
- `common/src/test/java/com/liskovsoft/smartyoutubetv2/common/ai/subtitle/integration/AiSubtitleCueBridgeTest.java` — strengthened re-enable assertions
- `docs/ai-subtitle/worker-plans/M02-fix-02-plan.md` (create before correction code)
- `docs/ai-subtitle/worker-reports/M02-report.md` — amended statements and final evidence

Do not create new production files. Do not modify any other existing SmartTube file.

## DO NOT MODIFY

- `.github/workflows/CI.yml`, `.github/workflows/ai-subtitle-validation.yml`
- Gradle files, dependency versions, or Android SDK levels
- Any production file other than the settings helper's line endings
- `PlayerData.java`, `PlaybackPresenter.java`, `SubtitleManager.java`, `AiSubtitleController.java`
- `MediaServiceCore`, `SharedModules`, `exoplayer-amzn-2.10.6`, or KissTranslator
- architecture, migration map, roadmap, upstream ledger, decisions, or the review record
- Git history; do not rebase, amend prior commits, force-push, or squash the accepted evidence chain

Do not reformat, reflow, rename, or restyle anything: the settings-helper change is a pure line-terminator repair, and the test change adds assertions without weakening existing ones.

## EXISTING INTERFACES

- Preserve every M02 contract: `TranslationProvider`, `TranslationCall`, `TranslationCallback`, `TranslationRequest`, `TranslationResult`, `AiSubtitleData`, `AiSubtitleCueBridge.process(List<Cue>)`, `onEnabledChanged(boolean)`, and the one-cue renderer hook.
- Preserve the fake output exactly: `[ZH] ` plus the exact source text.
- Preserve generation, epoch, request-id, cancellation, deduplication, pause, release, and source-only fallback semantics.

## BEHAVIOR REQUIREMENTS

1. `SubtitleSettingsPresenter.java` contains zero LF-only line terminators after the correction; only the added method's 12 lines change, and the Java semantics are byte-identical apart from the terminator.
2. After disable and re-enable, a callback delivered for the post-re-enable request must complete a translation: a subsequent `process(cues("Hello"))` returns exactly `Hello\n[ZH] Hello`, and no third request is issued (the completed result is cached).
3. The strengthened test must fail if the post-re-enable callback is rejected — the worker demonstrates this by removing the delivery step, observing the failure, and restoring it.
4. `M02-report.md` states only what the automated suites actually cover, names the lifecycle events that are covered, and keeps background/foreground, PiP, and the whole device matrix explicitly `NOT RUN`.
5. The report no longer claims the settings file was fully restored to its original style unless the line-ending repair actually makes that true.
6. No new lint error, no new source file, no wider upstream patch surface, and no change to M02 semantics.

## STATE / INVARIANTS

- Disabled path unchanged; source-only fallback unchanged.
- No callback may bypass generation/request/epoch identity checks.
- The three-file upstream patch surface established by M02 is unchanged.

## EDGE CASES

- repeated disable/re-enable without an intervening cue;
- a provider that ignores cancellation delivering after disable, and then again after re-enable;
- cache hit on the post-re-enable rendering opportunity;
- accidental whole-file conversion while repairing line endings (must not happen: verify byte-wise, not with `grep -c`).

## TEST REQUIREMENTS

Add or revise automated tests that prove:

1. the post-re-enable callback completes a translation and renders the exact dual-line cue;
2. the post-re-enable request count stays at 2 (no duplicate request when the cached result is available);
3. all previously passing bridge/controller/provider tests remain green;
4. the strengthened test is genuinely able to fail (mutation evidence recorded in the report).

Do not weaken assertions or convert failures into ignored/assumed tests.

## COMMANDS TO RUN

GitHub Actions remains authoritative. The final workflow must provide evidence for:

```text
JDK 11 supplementary job:
./gradlew :common:testStbetaDebugUnitTest --tests com.liskovsoft.smartyoutubetv2.common.ai.subtitle.settings.AiSubtitleDataTest

JDK 17 primary job, in order:
./gradlew :common:testStbetaDebugUnitTest
./gradlew lintStbetaRelease
./gradlew assembleStbetaRelease
```

Local runs are diagnostic only. Before pushing, run `git diff --check`, confirm the settings file's byte-level line endings, and confirm the diff touches only the authorized files.

## UPSTREAM IMPACT

`SubtitleSettingsPresenter.java` remains the only modified existing SmartTube file, and this package changes only its line terminators — no new hook, no new patch budget.

## ACCEPTANCE CRITERIA

1. The settings helper contains 0 LF-only lines and its diff is limited to the 12 corrected terminators.
2. The strengthened re-enable test proves completion, is green, and provably fails without the post-re-enable delivery.
3. The report's line-ending statement and automation-coverage statement are accurate; background/foreground, PiP, and the device matrix stay `NOT RUN`.
4. All 34 JVM tests pass on the JDK 17 lane (bridge 19, controller 10, provider 5) with the preference suite still executing in the JDK 11 job.
5. A replacement CI run is green for the final product SHA, with every step outcome and artifact recorded.
6. Only authorized files changed; M03 remains untouched.

## COMMIT REQUIREMENT

Use at most two additional logical commits:

```text
fix(ai-subtitle): address M02 second-pass review
docs(ai-subtitle): finalize M02 second-pass validation
```

The first commit contains the code, test, and documentation corrections (including this package's plan). The second contains only the final report revision after CI evidence exists. Do not rewrite prior history.

## WORKER PLAN REQUIREMENT

Before correction code, create `docs/ai-subtitle/worker-plans/M02-fix-02-plan.md` containing:

- starting HEAD and dirty state;
- the exact byte-level evidence for the mixed line endings;
- the design for the line-ending repair and the strengthened test;
- exact files and tests, including the mutation check;
- commit/push/CI sequence;
- risks and stop conditions.

## WORKER REPORT REQUIREMENT

Amend `docs/ai-subtitle/worker-reports/M02-report.md`; do not create a competing M02 report. Add:

- the second-pass disposition and the path of the review record;
- this plan's path and the correction commits;
- each of the three findings' root cause and fix;
- the line-ending verification output;
- the strengthened test, its real execution counts, and the mutation evidence;
- replacement CI run URL, run ID, final product SHA, step outcomes, and artifact names;
- updated acceptance tables;
- confirmation that M03 was not started.

Return the amended report and final commit range once. Do not continue beyond M02.
